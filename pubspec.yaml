name: flutter_finance_dashboard
description: Flutter finance dashboard — portfolio overview, stock holdings, budget tracking, transactions, spending analytics, goals, watchlist
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
